<UserControl x:Class="QuizGame.Views.EditQuizView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="800">

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Quiz picker -->
        <StackPanel Grid.Column="0" Grid.Row="0">
            <TextBlock Text="Select quiz" FontWeight="Bold"/>
            <ComboBox ItemsSource="{Binding Quizzes}"
                      DisplayMemberPath="Name"
                      SelectedItem="{Binding SelectedQuiz}" />
        </StackPanel>

        <!-- Questions list -->
        <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,12,0,0">
            <TextBlock Text="Questions" FontWeight="Bold"/>
            <ListBox ItemsSource="{Binding Questions}"
                     SelectedItem="{Binding SelectedQuestion}"
                     DisplayMemberPath="Text" Height="360"/>
            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <Button Content="Add" Width="70" Click="Add_Click"/>
                <Button Content="Delete" Width="70" Margin="8,0,0,0" Click="Delete_Click"/>
                <Button Content="Apply changes" Width="130" Click="Apply_Click"/>

            </StackPanel>
        </StackPanel>

        <!-- Edit form -->
        <ScrollViewer Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Edit question" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>

                <TextBlock Text="Question text:"/>
                <TextBox Text="{Binding EditText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Text="Answers:" FontWeight="Bold" Margin="0,8,0,4"/>
                <TextBox Text="{Binding Answer1, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,4" />
                <TextBox Text="{Binding Answer2, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,4" />
                <TextBox Text="{Binding Answer3, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                <TextBlock Text="Correct index (0..2):"/>
                <TextBox Text="{Binding CorrectIndex, UpdateSourceTrigger=PropertyChanged}" Width="60" />

                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                    <Button Content="Apply changes" Width="130" Click="Apply_Click"/>
                    <Button Content="Save all" Width="100" Margin="8,0,0,0" Click="SaveAll_Click"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- status -->
        <TextBlock Grid.Column="2" Grid.Row="2" Text="Saved to %LocalAppData%\\QuizGame\\quizzes.json"
                   Foreground="Gray" Margin="0,8,0,0"/>
    </Grid>
</UserControl>
